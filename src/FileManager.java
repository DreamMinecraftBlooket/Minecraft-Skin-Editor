import java.awt.*;
import javax.swing.*;

public class FileManager extends JPanel {
    public FileManager() {
        this.setBackground(Color.WHITE);
        this.add(new JLabel("File Explorer"));
    }
}
